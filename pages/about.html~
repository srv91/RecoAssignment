<!-- about.html -->
    <div class="jumbotron text-center">
        <h1>About Page</h1>

          <div class="row">
            <table datatable="ng" class="row-border hover">
              <thead>
                <tr>
                  <th>Title</th>
                  <th>Views</th>
                </tr>
              </thead>
              <tbody>
                <tr ng-repeat="video in video_array">
                  <td>{{video.title}}</td>
                  <td>{{video.stats}}</td>
                </tr>
              </tbody>
            </table>
          </div>
    </div>

